version: '3.8'

services:
  urmoney:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - urmoney-data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3001

volumes:
  urmoney-data:
